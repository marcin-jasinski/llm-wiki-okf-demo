# Task: LLM backend credentials & model choice

Labels: wayfinder:task
Status: ready-for-human
Blocked-by: —
Map: ../MAP.md

## Question

Human checklist: confirm an OpenRouter API key is available (and/or LM Studio installed with a function-calling-capable local model), pick the default `MODEL_NAME` for each backend, and place credentials in `.env` (gitignored). Resolution records which backends are live for development and the chosen models.
